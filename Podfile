# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OpenGL ES Test' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for OpenGL ES Test

  target 'OpenGL ES TestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OpenGL ES TestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
